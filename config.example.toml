# ============================================================================
# SERVER WHITELIST (Private Bot Mode)
# ============================================================================
# List of server (guild) IDs allowed to use this bot
# Leave empty [] to allow ALL servers (public mode)
# Get server ID: Right-click server icon → Copy Server ID (need Developer Mode enabled)
ALLOWED_GUILD_IDS = []  # Example: [123456789, 987654321]

# ============================================================================
# MONITORED CHANNELS
# ============================================================================
# List of channel IDs where the bot will scan for images
# Leave empty [] to monitor ALL channels in allowed servers
# Get channel ID: Right-click channel → Copy Channel ID
MONITORED_CHANNEL_IDS = []

SCAN_LIMIT_BYTES = 104857600 # 10MiB
A1111_IMPORTANT_FIELDS = [
    "Prompt",
    "Negative Prompt",
    "Steps",
    "Sampler",
    "CFG scale",
    "Seed",
    "Size",
    "Model",
    "VAE",
    "Denoising strength",
    "Hires upscale",
    "Hires steps",
    "Hires upscaler",
    "Version",
]
A1111_PROMPT_SIZE_LIMIT=1000
COMFYUI_SHOW_TITLES=true
COMFYUI_EXTRACT_WIDGET_VALUES=true
COMFYUI_PRIORITIZE_NODES_WITH_TEXT=true
COMFYUI_REPLACE_FIELD_NEWLINES=true
MESSAGE_EMBED_LIMIT=25
ATTACH_FILE_SIZE_THRESHOLD=1980
REACT_ON_NO_METADATA=false
EMOJI_METADATA_FOUND="🔎"
EMOJI_NO_METADATA="⛔"
LOG_COLOR=false
LOG_LEVEL="INFO"
